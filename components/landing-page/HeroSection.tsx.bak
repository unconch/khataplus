"use client"

import Link from "next/link"
import { ArrowRight, Play, TrendingUp, DollarSign, Package } from "lucide-react"
import { Navbar } from "./Navbar"
import { AdvancedScrollReveal } from "@/components/advanced-scroll-reveal"
import { motion, Variants } from "framer-motion"
import { cn } from "@/lib/utils"

interface HeroSectionProps {
 isAuthenticated: boolean
 orgSlug?: string | null
 isGuest?: boolean
}

// Kinetic Typography variants
const containerVariants: Variants = {
 hidden: { opacity: 0 },
 visible: {
 opacity: 1,
 transition: { staggerChildren: 0.1, delayChildren: 0.2 }
 }
}

const childVariants: Variants = {
 hidden: { opacity: 0, y: 50, rotateX: -90 },
 visible: {
 opacity: 1,
 y: 0,
 rotateX: 0,
 transition: { type: "spring", bounce: 0.4, duration: 1.2 } as any
 }
}

export function HeroSection({ isAuthenticated }: HeroSectionProps) {
 const primaryHref = isAuthenticated ? "/dashboard" : "/auth/sign-up"
 const secondaryHref = "/demo/dashboard"

 return (
 <>
 <Navbar isAuthenticated={isAuthenticated} isLight />

 <section className="relative pt-24 md:pt-32 pb-20 bg-transparent text-zinc-900 min-h-[90vh] flex flex-col items-center justify-center">
 {/* Sarvam-style Aurora Background */}
 <div className="absolute inset-0 overflow-visible pointer-events-none">
 <div className="absolute top-[-20%] left-1/2 -translate-x-1/2 w-[120%] h-[800px] flex justify-center opacity-40 mix-blend-multiply filter blur-[100px] md:blur-[140px]">
 <motion.div
 animate={{
 scale: [1, 1.1, 1],
 opacity: [0.6, 0.8, 0.6],
 rotate: [0, 5, 0]
 }}
 transition={{ duration: 15, repeat: Infinity, ease: "easeInOut" }}
 className="absolute top-0 w-[45%] h-full bg-blue-300 rounded-[100%]"
 />
 <motion.div
 animate={{
 scale: [1, 1.2, 1],
 opacity: [0.5, 0.7, 0.5],
 rotate: [0, -5, 0]
 }}
 transition={{ duration: 20, repeat: Infinity, ease: "easeInOut", delay: 2 }}
 className="absolute top-0 right-1/4 w-[40%] h-full bg-violet-300 rounded-[100%]"
 />
 <motion.div
 animate={{
 scale: [1, 1.1, 1],
 opacity: [0.4, 0.6, 0.4]
 }}
 transition={{ duration: 18, repeat: Infinity, ease: "easeInOut", delay: 4 }}
 className="absolute top-1/4 left-1/4 w-[35%] h-[80%] bg-emerald-200 rounded-[100%]"
 />
 </div>
 </div>

 <div className="relative z-10 max-w-5xl mx-auto px-6 w-full">
 {/* Centered Ultra-Minimalist Hero Content */}
 <div className="flex flex-col items-center justify-center text-center space-y-10">

 {/* Tagline / Badge */}
 <motion.div
 initial={{ opacity: 0, y: 20 }}
 animate={{ opacity: 1, y: 0 }}
 transition={{ duration: 0.8, delay: 0.2, ease: "easeOut" }}
 className="inline-flex items-center gap-2 px-4 py-2 rounded-full border border-zinc-200/60 bg-white shadow-sm"
 >
 <span className="flex h-2 w-2 rounded-full bg-emerald-500" />
 <span className="text-xs font-semibold uppercase tracking-widest text-zinc-600">The Future of Indian Business</span>
 </motion.div>

 {/* Kinetic Typography Title */}
 <motion.h1
 variants={containerVariants}
 initial="hidden"
 animate="visible"
 className="text-[3.5rem] sm:text-[4.5rem] md:text-[5.5rem] lg:text-[7rem] font-medium tracking-tighter leading-[0.9] text-zinc-950"
 >
 <span className="block overflow-hidden pb-2">
 <motion.span variants={childVariants} className="inline-block">Command your</motion.span>
 </span>
 <span className="block overflow-hidden pb-4">
 <motion.span variants={childVariants} className="inline-block text-zinc-400 font-light italic pr-4">business,</motion.span>
 <motion.span variants={childVariants} className="inline-block">beautifully.</motion.span>
 </span>
 </motion.h1>

 <motion.div
 initial={{ opacity: 0, y: 20 }}
 animate={{ opacity: 1, y: 0 }}
 transition={{ duration: 0.8, delay: 0.6, ease: "easeOut" }}
 >
 <p className="max-w-2xl text-xl font-light leading-relaxed text-zinc-500 tracking-wide mx-auto">
 The architectural standard for GST billing, automated stock tracking, and digital credit records in India.
 </p>
 </motion.div>

 <motion.div
 initial={{ opacity: 0, y: 20 }}
 animate={{ opacity: 1, y: 0 }}
 transition={{ duration: 0.8, delay: 0.8, ease: "easeOut" }}
 >
 <div className="flex flex-col gap-4 sm:flex-row sm:items-center justify-center pt-6">
 <Link
 href={primaryHref}
 className="inline-flex items-center justify-center gap-4 rounded-full bg-zinc-950 px-10 py-5 text-sm font-semibold text-white shadow-xl hover:bg-zinc-800 transition-all active:scale-[0.98]"
 >
 Get Started Free
 <ArrowRight className="h-4 w-4" />
 </Link>
 <Link
 href={secondaryHref}
 className="inline-flex items-center justify-center gap-4 rounded-full border border-zinc-200 bg-white px-10 py-5 text-sm font-semibold text-zinc-800 transition hover:bg-white hover:border-zinc-300 hover:shadow-sm"
 >
 Login to Dashboard
 </Link>
 </div>
 </motion.div>

 <motion.div
 initial={{ opacity: 0 }}
 animate={{ opacity: 1 }}
 transition={{ duration: 1, delay: 1.2 }}
 className="pt-16 pb-8 flex flex-wrap justify-center gap-x-12 gap-y-6 text-sm font-medium text-zinc-400 uppercase tracking-widest"
 >
 <span>GST & Non-GST</span>
 <span>Credit Reminders</span>
 <span>Offline Mode</span>
 <span>Profit Reports</span>
 </motion.div>
 </div>
 </div>

 <div className="pointer-events-none absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-t from-[#fafafa] to-transparent z-30" />

 <style jsx global>{`
 .perspective-1000 { perspective: 1000px; }
 `}</style>
 </section>
 </>
 )
}
